apiVersion: v1
kind: Service
metadata:
  name: {{ include "spanforge.fullname" . }}
  labels:
    {{- include "spanforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spanforge.selectorLabels" . | nindent 4 }}
